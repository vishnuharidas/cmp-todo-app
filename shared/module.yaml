product:
  type: lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - $compose.foundation: exported
  - $compose.material3: exported
  - org.jetbrains.androidx.lifecycle:lifecycle-viewmodel-compose:2.9.6  # ViewModel support for Compose

settings:
  compose:
    enabled: true